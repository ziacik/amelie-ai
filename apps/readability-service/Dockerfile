# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build readability-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t readability-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system readability-service && \
          adduser --system -G readability-service readability-service

COPY dist/apps/readability-service readability-service
RUN chown -R readability-service:readability-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix readability-service --omit=dev -f install

CMD [ "node", "readability-service" ]
